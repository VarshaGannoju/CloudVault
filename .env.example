# Used by docker-compose.yml to configure the Postgres container and
# to bake the API URL into the frontend build.
DB_NAME=cloudvault
DB_USER=postgres
DB_PASSWORD=postgres
VITE_API_BASE_URL=http://localhost:5000/api
