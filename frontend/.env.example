# Copy to .env and adjust as needed. Vite only exposes vars prefixed with VITE_.
VITE_API_BASE_URL=http://localhost:5000/api
# Production public share link base (defaults to window.location.origin in dev)
# VITE_PUBLIC_SHARE_BASE=https://cloudvault.com
