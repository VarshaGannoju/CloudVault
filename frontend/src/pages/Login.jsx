// Placeholder page. Full login form + validation wired up to the
// backend /api/auth/login endpoint in the Authentication step.
export default function Login() {
  return (
    <div className="container py-5">
      <h1>Log in to CloudVault</h1>
      <p className="text-secondary">Login form coming in the Authentication step.</p>
    </div>
  );
}
