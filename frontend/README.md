# CloudVault - Frontend

React (Vite) frontend for CloudVault. See the [root README](../README.md) for full project setup, architecture, and environment variable documentation.

Quick start:

```bash
cp .env.example .env
npm install
npm run dev
```
